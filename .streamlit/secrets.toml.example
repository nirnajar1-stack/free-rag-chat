# Copy to secrets.toml for local Streamlit, or paste into
# Streamlit Cloud → App settings → Secrets / HF Spaces → Settings → Secrets

GROQ_API_KEY = "your_groq_api_key_here"
DOCS_FOLDER_PATH = "data/docs"
